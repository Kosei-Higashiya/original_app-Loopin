# SMTP Configuration for Password Reset Emails
# 
# For Gmail:
# 1. Enable 2-factor authentication in your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated password for SMTP_PASSWORD
#
# For other SMTP providers, adjust the settings accordingly

# SMTP server address (e.g., smtp.gmail.com for Gmail, smtp.sendgrid.net for SendGrid)
SMTP_ADDRESS=smtp.gmail.com

# SMTP port (587 for TLS/STARTTLS, 465 for SSL)
SMTP_PORT=587

# Your domain name
SMTP_DOMAIN=original-app-loopin.onrender.com

# SMTP username (usually your email address)
SMTP_USERNAME=your-email@gmail.com

# SMTP password (for Gmail, use App Password)
SMTP_PASSWORD=your-app-password-here

# Authentication method (plain, login, or cram_md5)
SMTP_AUTHENTICATION=plain

# Enable STARTTLS (true for most providers)
SMTP_ENABLE_STARTTLS_AUTO=true

# Google OAuth2 credentials (if using Google login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
