<div class="dashboard-section">
  <div class="container">
    <h1 class="dashboard-title">📌 みんなの習慣一覧</h1>
    <% if @current_tag.present? %>
      <p class="dashboard-subtitle">タグ「#<%= @current_tag %>」の投稿を表示中
        <%= link_to "すべて表示", posts_path, class: "btn btn-outline-secondary btn-sm ms-2" %>
      </p>
    <% else %>
      <p class="dashboard-subtitle">コミュニティのメンバーが共有している習慣の取り組みをご覧ください</p>
    <% end %>

    <div class="mb-4">
      <div class="search-layout-container">
        <div class="search-buttons-group">
          <%= link_to "投稿する", new_post_path, class: "btn btn-primary btn-lg" %>
          <% if user_signed_in? %>
            <%= link_to "いいねした投稿", liked_posts_path, class: "btn btn-outline-primary" %>
          <% end %>
          <%= link_to "ダッシュボードに戻る", dashboard_path, class: "btn btn-primary" %>
        </div>

        <!-- 検索フォーム -->
        <div class="search-form-wrapper">
          <%= search_form_for @q, url: posts_path, class: "search-form-flex" do |f| %>
            <%= f.search_field :content_or_habit_title_or_tags_name_cont,
                placeholder: "投稿内容、習慣タイトル、タグで検索...",
                class: "form-control search-input",
                value: params.dig(:q, :content_or_habit_title_or_tags_name_cont) %>
            <%= f.submit "検索", class: "btn btn-primary search-btn" %>
            <% if params[:q].present? && params[:q][:content_or_habit_title_or_tags_name_cont].present? %>
              <%= link_to "クリア", posts_path, class: "btn btn-outline-secondary search-btn" %>
            <% end %>
          <% end %>
        </div>
      </div>

    <div id="posts-container">
      <% if @posts.any? %>
        <div class="dashboard-grid">
          <% @posts.each do |post| %>
            <div class="dashboard-card" id="post-<%= post.id %>">
              <div class="card-header">
                <h3 class="card-title">
                  🎯 <%= post.habit.title %>
                </h3>
                <small class="text-white-50">
                  👤 <%= post.user.display_name%> •
                  ⏰ <%= time_ago_in_words(post.created_at) %>前
                </small>
              </div>
              <div class="card-content">
                <% if post.tags.any? %>
                  <div class="mb-3">
                    <% post.tags.each do |tag| %>
                      <%= link_to "##{tag.name}", posts_path(tag: tag.name),
                          class: "badge bg-primary text-decoration-none me-2 mb-1",
                          style: "font-size: 0.9rem; padding: 0.5rem 0.75rem;" %>
                    <% end %>
                  </div>
                <% end %>
                <p style="font-size: 1rem; line-height: 1.6; margin-bottom: 1.5rem;">
                  <%= simple_format(truncate(post.content, length: 39, separator: ' ')) %>
                </p>
                <div class="d-flex justify-content-between align-items-center">
                  <%= render 'shared/like_button', post: post %>
                  <% if post.user == current_user %>
                    <div class="card-actions">
                      <%= link_to "編集", edit_post_path(post), class: "btn btn-warning btn-sm" %>
                      <%= link_to "削除", post_path(post),
                          data: {
                            turbo_method: :delete,
                            turbo_confirm: "本当に削除しますか？"
                          },
                          class: "btn btn-danger btn-sm" %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-5">
          <div class="dashboard-card" style="max-width: 500px; margin: 40px auto;">
            <div class="card-header">
              <% if params[:q].present? && params[:q][:content_or_habit_title_or_tags_name_cont].present? %>
                <h3 class="card-title">🔍 検索結果がありません</h3>
              <% else %>
                <h3 class="card-title">📝 まだ投稿がありません</h3>
              <% end %>
            </div>
            <div class="card-content">
              <% if params[:q].present? && params[:q][:content_or_habit_title_or_tags_name_cont].present? %>
                <p>検索条件に該当する投稿がありません。<br>別のキーワードで検索してみてください。</p>
              <% else %>
                <p>最初の投稿をして、コミュニティを盛り上げませんか？</p>
              <% end %>
              <div class="card-actions">
                <% if params[:q].present? && params[:q][:content_or_habit_title_or_tags_name_cont].present? %>
                  <%= link_to "すべての投稿を見る", posts_path, class: "btn btn-primary" %>
                <% else %>
                  <%= link_to "投稿する", new_post_path, class: "btn btn-primary" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
