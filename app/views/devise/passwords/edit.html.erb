<div class="container-fluid mt-4 px-4">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6 col-xl-5">
      <div class="card shadow-lg border-2" style="border-color: #007bff;">
        <div class="card-header bg-primary text-white text-center py-4">
          <h1 class="card-title mb-2" style="font-size: 2rem;">🔑 新しいパスワード設定</h1>
          <p class="mb-0" style="font-size: 1.1rem;">新しいパスワードを設定してください</p>
        </div>
        <div class="card-body p-5">
          <%= form_with(model: resource, as: resource_name, url: password_path(resource_name), html: { method: :put }, local: true) do |form| %>
            <% if resource.errors.any? %>
              <div class="alert alert-danger">
                <h6><%= pluralize(resource.errors.count, "つのエラー") %>があります:</h6>
                <ul class="mb-0">
                  <% resource.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <%= form.hidden_field :reset_password_token %>

            <div class="mb-4">
              <%= form.label :password, "新しいパスワード", class: "form-label fw-bold", style: "font-size: 1.2rem;" %>
              <% if @minimum_password_length %>
                <small class="form-text text-muted">(<%= @minimum_password_length %> 文字以上)</small>
              <% end %>
              <div class="mt-2">
                <%= form.password_field :password, autofocus: true, autocomplete: "new-password",
                    class: "form-control form-control-lg",
                    placeholder: "新しいパスワードを入力してください",
                    style: "font-size: 1.1rem; padding: 1rem;" %>
              </div>
            </div>

            <div class="mb-4">
              <%= form.label :password_confirmation, "新しいパスワード確認", class: "form-label fw-bold", style: "font-size: 1.2rem;" %>
              <div class="mt-2">
                <%= form.password_field :password_confirmation, autocomplete: "new-password",
                    class: "form-control form-control-lg",
                    placeholder: "新しいパスワードを再度入力してください",
                    style: "font-size: 1.1rem; padding: 1rem;" %>
              </div>
            </div>

            <div class="d-grid mb-4">
              <%= form.submit "パスワードを変更", class: "btn btn-primary btn-lg",
                  style: "font-size: 1.2rem; padding: 1rem; background-color: #007bff; border: none;" %>
            </div>
          <% end %>

          <div class="text-center">
            <div class="mb-3">
              <%= link_to "ログインページに戻る", new_session_path(resource_name),
                  class: "btn btn-outline-primary btn-lg",
                  style: "font-size: 1.1rem; padding: 0.75rem 2rem;" %>
            </div>

            <div class="mb-2">
              <%= link_to "新規登録はこちら", new_registration_path(resource_name),
                  class: "text-decoration-none", style: "color: #6c757d;" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
