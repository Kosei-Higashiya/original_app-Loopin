<div class="habit-records-section">
  <div class="container">
    <h1 class="page-title">みんなの習慣</h1>
    <p class="page-subtitle">コミュニティのメンバーが共有している習慣記録です</p>

    <div class="actions mb-4">
      <%= link_to "習慣を投稿する", new_habit_record_path, class: "btn btn-primary" %>
      <%= link_to "ダッシュボードに戻る", dashboard_path, class: "btn btn-secondary" %>
    </div>

    <% if @habit_records.any? %>
      <div class="habit-records-grid">
        <% @habit_records.each do |record| %>
          <div class="habit-record-card">
            <div class="card-header">
              <h3 class="record-title"><%= record.title %></h3>
              <div class="record-meta">
                <small class="text-muted">
                  <%= record.user.email %> • <%= record.recorded_at.strftime('%Y年%m月%d日') %>
                </small>
              </div>
            </div>
            <div class="card-content">
              <% if record.content.present? %>
                <p class="record-content"><%= simple_format(record.content) %></p>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="empty-state">
        <p>まだ習慣記録がありません。</p>
        <%= link_to "最初の習慣を投稿する", new_habit_record_path, class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>