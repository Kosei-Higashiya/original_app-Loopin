<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>あなたの習慣</h1>
    <div class="d-flex gap-2">
      <%= link_to "カレンダー", calendar_habits_path, class: "btn btn-success" %>
      <%= link_to "新しい習慣を作成", new_habit_path, class: "btn btn-primary" %>
    </div>
  </div>

  <% if @habits.any? %>
    <div class="row">
      <% @habits.each do |habit| %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= habit.title %></h5>
              <p class="card-text"><%= truncate(habit.description, length: 100) %></p>
              <p class="text-muted small">
                作成日: <%= habit.created_at.strftime("%Y年%m月%d日") %>
              </p>
              <div class="d-flex gap-2">
                <%= link_to "詳細", habit, class: "btn btn-outline-primary btn-sm" %>
                <%= link_to "編集", edit_habit_path(habit), class: "btn btn-outline-secondary btn-sm" %>
                <%= link_to "削除", habit,
                        data: {
                            turbo_method: :delete,
                            turbo_confirm: "本当に削除しますか？"
                        },
                        class: "btn btn-outline-danger btn-sm" %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-5">
      <h3 class="text-muted">まだ習慣が登録されていません</h3>
      <p class="text-muted">新しい習慣を作成して、継続の記録を始めましょう！</p>
      <%= link_to "最初の習慣を作成", new_habit_path, class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
