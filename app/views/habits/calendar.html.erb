<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>習慣カレンダー</h1>
    <div class="btn-group" role="group">
      <%= link_to "習慣一覧", habits_path, class: "btn btn-outline-primary" %>
      <%= link_to "新しい習慣を作成", new_habit_path, class: "btn btn-primary" %>
    </div>
  </div>

  <% if @habits.any? %>
    <div class="card">
      <div class="card-body">
        <% begin %>
          <%= month_calendar(events: @habit_records, attribute: :recorded_at) do |date, records| %>
            <%= date.day %>
            
            <% if records.any? %>
              <div class="calendar-events mt-2">
                <% records.each do |record| %>
                  <div class="calendar-event <%= record.completed? ? 'completed' : 'incomplete' %>"
                       title="<%= record.habit.title %> - <%= record.completed? ? '完了' : '未完了' %>">
                    <small class="d-block">
                      <%= truncate(record.habit.title, length: 12) %>
                      <%= habit_status_badge(record) %>
                    </small>
                  </div>
                <% end %>
              </div>
            <% end %>
          <% end %>
        <% rescue NameError %>
          <div class="alert alert-warning">
            <h5>カレンダー機能の準備中</h5>
            <p>カレンダー表示には <code>simple_calendar</code> gemのインストールが必要です。</p>
            <p>以下のコマンドを実行してください:</p>
            <pre><code>bundle install</code></pre>
          </div>
          
          <!-- Fallback: Simple list view -->
          <div class="mt-4">
            <h5>今月の習慣記録</h5>
            <% @habit_records.where(recorded_at: Date.current.beginning_of_month..Date.current.end_of_month).order(:recorded_at).each do |record| %>
              <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                <div>
                  <strong><%= record.habit.title %></strong>
                  <small class="text-muted d-block"><%= record.recorded_at.strftime("%Y年%m月%d日") %></small>
                </div>
                <%= habit_status_badge(record) %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="row mt-4">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h5 class="card-title mb-0">習慣一覧</h5>
          </div>
          <div class="card-body">
            <% @habits.each do |habit| %>
              <div class="d-flex justify-content-between align-items-center mb-2">
                <span>
                  <%= link_to habit.title, calendar_habit_path(habit), 
                      class: "text-decoration-none" %>
                </span>
                <span class="badge <%= habit.active? ? 'bg-success' : 'bg-secondary' %>">
                  <%= habit.active? ? 'アクティブ' : '非アクティブ' %>
                </span>
              </div>
            <% end %>
          </div>
        </div>
      </div>
      
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h5 class="card-title mb-0">カレンダーの見方</h5>
          </div>
          <div class="card-body">
            <div class="mb-2">
              <span class="badge bg-success">✓</span> 完了した習慣
            </div>
            <div class="mb-2">
              <span class="badge bg-warning">○</span> 未完了の習慣
            </div>
            <p class="text-muted small mb-0">
              日付をクリックして習慣を記録できます。
            </p>
          </div>
        </div>
      </div>
    </div>
  <% else %>
    <div class="text-center py-5">
      <h3 class="text-muted">まだ習慣が登録されていません</h3>
      <p class="text-muted">新しい習慣を作成して、カレンダーで管理を始めましょう！</p>
      <%= link_to "最初の習慣を作成", new_habit_path, class: "btn btn-primary" %>
    </div>
  <% end %>
</div>