<div class="container mx-auto px-4 py-8">
  <div class="max-w-md mx-auto bg-white shadow-lg rounded-lg overflow-hidden">
    <div class="text-center p-6">
      <div class="text-6xl mb-4"><%= @badge.icon %></div>
      <h1 class="text-2xl font-bold text-gray-800 mb-2"><%= @badge.name %></h1>
      <p class="text-gray-600 mb-4"><%= @badge.description %></p>
      
      <div class="bg-gray-100 rounded-lg p-4 mb-4">
        <h3 class="font-semibold text-gray-700 mb-2">獲得条件</h3>
        <p class="text-gray-600">
          <%= @badge.condition_type_name %>: <%= @badge.condition_value %>
        </p>
      </div>

      <% if current_user.has_badge?(@badge) %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
          <strong>おめでとうございます！</strong> このバッジを獲得済みです。
        </div>
      <% else %>
        <div class="bg-yellow-100 border border-yellow-400 text-yellow-700 px-4 py-3 rounded">
          このバッジはまだ獲得していません。
        </div>
      <% end %>
    </div>
  </div>

  <% if @users_with_badge.any? %>
    <div class="max-w-md mx-auto mt-8">
      <h3 class="text-lg font-semibold mb-4 text-center">このバッジを獲得したユーザー</h3>
      <div class="bg-white shadow rounded-lg p-4">
        <% @users_with_badge.each do |user| %>
          <div class="flex items-center justify-between py-2">
            <span class="text-gray-700"><%= user.display_name %></span>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <div class="text-center mt-8">
    <%= link_to "バッジ一覧に戻る", badges_path, class: "text-blue-500 hover:text-blue-700" %>
  </div>
</div>